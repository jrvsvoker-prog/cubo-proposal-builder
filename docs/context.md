# Динамическое КП · рабочее состояние

> Вход для агента: [`../AGENTS.md`](../AGENTS.md) → регламент
> [`dynamic-proposal/agent-runbook.md`](dynamic-proposal/agent-runbook.md).
> Для менеджера: [`../README.md`](../README.md).
> Статус на 22.09: v1 ПРИНЯТА (тег `builder-v1` на `5d90498e`), доработка v1.4.
> Выдача: ZIP, HTML «Мосгорбайк», `RELEASE.md` с хешами.
> Холодное открытие без сети: пользователь подтвердил 22.09.
> Логотипы: RAND — только шаблонный SVG (красится под фон, без плашки и без
> файла из сводки); клиент без плашки, `coverPlate` только если инверсия портит знак.

Рабочий код: эта копия, ветка `codex/kp-wave2`.
Выдача кандидата: папка `cubo-builder-v1-candidate` (ZIP Builder,
HTML «Мосгорбайк», `RELEASE.md` с хешами). Старые планы, порты и папки
в `Documents/Codex` — история, по ним работу не продолжать.

Открыто только это:

- push, merge и публикация — отдельное поручение, не начинать сами.

17.09 приняты обложка «Игривого» и финал Flood×Стори
(`85a94485`, тег `kp-mvp-shell-approved-2026-09-17`). Волны 12/14/18-A и
13/15/16/21/22 входят в эту ветку. Этап 4 проверен на HTML, действиях 01–06
и сценарии «сайт → КП» «Мосгорбайк»; диалог «Горизонт» отложен в пользу сайта.
18-B перенесён из checkpoint `9dfde9cf`, включая правку раскрытого фильтра.
Правила логотипов, коротких ролей и обязательного поиска primary — в runbook.
Акцент «Мосгорбайка» `#3633cd`, роль «Администратор». Без ориентиров бренда —
синий `#0369a1`. Генератор сам сайт не ищет.

## Рабочая копия

- Эта папка самодостаточна: установка, генерация и сборка не требуют
  соседних проектов. История интеграции и чекпоинты волн — в Git
  монорепозитория разработки; в переносимой выдаче их нет.
- Область работы: только динамическое КП внутри этой папки. Сайт
  документации, общий кит, токены и другие приложения не входят в задачу.
- Merge, push и публикация не разрешены.

## A · Шесть экранов

17.09 пользователь одобрил текущие шесть экранов и их варианты как базовую
десктопную версию. Адаптив теперь разрешён отдельному исполнителю (task-18),
границы данных проверяет task-15. Композицию сохранять, не начинать ревью заново.
03-E сохраняет ранее закреплённый layout (`769d330c`).

| Экран | Текущее решение |
|---|---|
| 01 · Дашборд | Текущая десктопная версия принята, включая 01-C |
| 02 · Аналитика | Текущая десктопная версия принята, включая 02-N/O |
| 03 · Карточка | Layout принят. Поля меняются по профилю |
| 04 · Заявки | Пайплайн, таблица и карточки приняты с поправкой: только просмотр записей |
| 05 · Подборки | Текущая десктопная версия принята |
| 06 · Кабинет | Текущая десктопная версия принята |

В 04 удалены кнопки перехода, столбец действий и локальная смена стадий.
Поиск, фильтры, сортировка и переключение вида сохранены. Это правило для
генерации КП: примеры представления данных без действий над отдельной записью.
Центровка логотипа исправлена (`8571a481`); 17.09 выровнены также иконки и плашки
свёрнутого меню: четыре экрана × school/service, 1440 и 390, раскрытие проверено.
Демо-primary вымышлены; реальный бренд приходит из вводных клиента.
Общий `templates-locked-v1` не ставился: итоговая визуальная приёмка открыта.
Техническая проверка адаптива не заменяет её.

## B · Оболочка КП

Рабочее направление второго G реализовано (`91948c15`): короткая обложка,
последовательные главы, граница «команда / клиенты», финальное действие.
В task-21 убраны оглавление, верхняя демоплашка и нижний дисклеймер.
Единственный клиентский вход: `index.html → src/main.ts → src/app/index.tsx`.
Показатели и ряд обложки берутся из модели дашборда.

Старый dev-вход `proposal.html`, семь его режимов и ненужный reveal-код убраны
из активной копии; сохранены в истории Git и у доноров. Технический `stand.html`
остаётся для проверок экранов и не является альтернативной оболочкой КП.

Принятая обложка — «Игривый» с крупной графикой; финал — текст слева,
анимированный интерфейс справа. Движение приостановлено вне видимости
и отключается при reduced-motion. Числа — из модели клиента.
Логотип (PNG/SVG → data URI) готовит агент по runbook; без файла — штатный
fallback. Один файл клиента на всех поверхностях: на обложке монохром белый,
внутри исходные цвета, без плашки. Цветной знак, который инверсия портит, —
`coverPlate` только на обложке. RAND — только шаблонный SVG, красится под фон,
без плашки, без фильтра и без файла из сводки.
При `proposal.partner` на обложке — пара «партнёр × клиент». Primary — из профиля.
Кикера «Для …» на обложке нет: имя уже в шапке. В текстах КП нет длинного тире.
Карточка 03: описание не дублирует услугу, есть параметры бизнеса, телефон не нули.
Главы: без подложки, крупный заголовок, убрана строка «Инструменты команды».
Пользователь одобрил направление и уточнил: сплошная primary-метка с номером
и названием, круглые плашки вокруг названий пунктов. Правка сделана;
её финальный вид ожидает приёмки пользователя на итоговом HTML.
Идея иконки остаётся отложенной полировкой.

## C · Генерация

Контракт данных и валидатор существуют. Генератор `scripts/profile-draft.mjs`
исправлен: новый профиль не копирует данные прежнего клиента, одна услуга валидна,
ошибки не перезаписывают результат. «Контур» повторно получен из сводки побайтно
без ручного изменения готового профиля (`91948c15`).

Task-12 связал KPI с ценой/масштабом сводки, прогресс — со стадией,
закрытые периоды — с `demoDate`; регрессии task-15 проверены в этапах 1–3.
Пример `runs/mayak-mvp/result.md`: одна услуга, нет филиалов, профиль воспроизводим.
22.09 подписи учитывают подразделения, предмет предложения и этапы из сводки;
нейтральные типы обращений заменили сервисную классификацию. Проверены school/service
с 0/1/3 подразделениями, отсутствие названия предложения и воспроизводимость «Маяка».
В runbook закреплены открытый вопрос и смысловая сверка до выдачи.
22.09 по официальному сайту «Мосгорбайка» собран и проверен отдельный пример:
одна услуга из прайса, официальный SVG, 1440/1024/390 и действия 01–06.
Найдено и исправлено «Мастерскаи»; новый тест воспроизвёл дефект до правки.
Журнал этапа 4 дополнен. В 18-B проверены таблица на 390, пять–шесть KPI,
длинные строки, логотипы и мобильная детализация. Холодный `file://` открыт.
Вход остаётся сообщением в чате; анкета и отдельный конструктор не планируются.
Поля `id`/`initials` зарезервированы пайплайном — не удалять как неиспользуемые.

## Запуск и показ

Запуск, адресная сборка клиентского профиля и проверка HTML из корня этой
папки описаны в [регламенте агента](dynamic-proposal/agent-runbook.md).
Перед запуском проверить порт; чужие процессы не останавливать.

Кандидат для приёмки: `http://127.0.0.1:5385/current/Mosgorbike.html`.
Этот адрес работает только в среде координатора с запущенным HTTP-сервером;
он не является входом в переносимый Builder и не доказывает cold file://.
Прежние показы на 5218/5361/5384 — исторические, для приёмки не использовать.
mehanika/kontur сохранены как технические примеры генератора; не предъявлять
их как новые бизнес-сценарии для визуального ревью.

## Проверки и ограничения

Исторические проверки первой волны `46a92707` не доказывают автономное
открытие нынешнего HTML: точный файл проверяется отдельно.
18-B принят по браузерной матрице 360/390/768/1024/1440 с указанными в отчёте
профилями. Физический телефон и экранная клавиатура не проверены.
Холодный `file://` пользователь подтвердил 22.09. Теги полного лока нет.

Демо локальное, без реальной авторизации, платежей и сохранения действий
после перезагрузки. Внешние контактные ссылки и tel не запускались.

Исторические доказательства — `docs/dynamic-proposal/reviews/` монорепозитория;
в переносимую папку они не входят. Приёмку пользователя не заменяют тесты.
